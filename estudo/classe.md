As classes são utilizadas para criar objetos, ela descreve o que o objeto será (é um modelo do objeto).

Ela é composta por:

- Nome
- Atributos (Propriedades, dados, variaveis internas)
- Metodos (Operações, comportamento)